<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".ui.driver_mode.fragments.sub_fragments.DriverStatusFragment">

    <LinearLayout
        android:id="@+id/driver_ord_confirmationLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center|center_horizontal"
        android:orientation="horizontal">

        <Button
            android:id="@+id/driver_ord_acceptButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center|center_horizontal|center_vertical"
            android:layout_margin="20dp"
            android:layout_weight="0"
            android:paddingLeft="30dp"
            android:paddingRight="30dp"
            android:text="Aceptar"
            android:textSize="20sp" />

        <Button
            android:id="@+id/driver_ord_cancelButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center|center_horizontal|center_vertical"
            android:layout_margin="20dp"
            android:layout_weight="0"
            android:backgroundTint="#BA4C4C"
            android:paddingLeft="30dp"
            android:paddingRight="30dp"
            android:text="Cancelar"
            android:textSize="20sp" />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/driver_ord_statusLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <Button
            android:id="@+id/driver_ord_nextStatusButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="20dp"
            android:text="Siguiente estado"
            android:textSize="20sp" />
    </LinearLayout>

</LinearLayout>